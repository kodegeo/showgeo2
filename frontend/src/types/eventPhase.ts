export enum EventPhase {
  PRE_LIVE = "PRE_LIVE",
  LIVE = "LIVE",
  POST_LIVE = "POST_LIVE",
}

export enum EventStatus {
  DRAFT = "DRAFT",
  SCHEDULED = "SCHEDULED",
  LIVE = "LIVE",
  COMPLETED = "COMPLETED",
  CANCELLED = "CANCELLED",
}

