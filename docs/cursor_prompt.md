The full system documentation for Showgeo 2.0 is in /docs.
Use system_architecture.md as the primary reference and follow the .cursorrules file at the root.

- The backend is in /backend (NestJS + Prisma + PostgreSQL).
- The frontend is in /frontend (React + Vite + TypeScript).
- Shared TypeScript types are in /shared.
All *_requirements.md files in /docs define model logic and data relationships (user, event, entity, store, tour, follow, etc.).


















