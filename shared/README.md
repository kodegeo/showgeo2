# Shared Types

This package contains shared TypeScript types and interfaces used across both the frontend and backend applications.

## Structure

- `types/` - Type definitions for all models (User, Entity, Event, Tour, Store, Follow, Geofencing)

## Usage

Import types from `@shared` in both frontend and backend:

```typescript
import { User, Entity, Event } from "@shared/types";
```

